/**
 * presets3d barrel
 *
 * Exports the two built-in 3D preset components (R009). These components
 * live inside a @react-three/fiber <Canvas> — do not use them outside R3F.
 */

export { OrbitPoints3D, type OrbitPoints3DProps } from './OrbitPoints3D';
export { Lattice3D, type Lattice3DProps } from './Lattice3D';
