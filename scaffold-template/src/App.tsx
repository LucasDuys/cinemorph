// Placeholder App. T009 (deck runtime) will replace this with an import of
// `./deck/Deck` once the Deck primitive lands.
export default function App() {
  return (
    <div className="bg-background text-foreground min-h-screen flex items-center justify-center">
      Deck mounts here (T009 will replace)
    </div>
  );
}
